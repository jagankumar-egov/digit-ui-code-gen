import React from "react";
import { useTranslation } from "react-i18next";
import { InboxSearchComposer } from "@egovernments/digit-ui-components";
import { useNavigate, useLocation } from "react-router-dom";
import {{camelCase config.entity.name}}InboxConfig from "../../configs/{{camelCase config.entity.name}}InboxConfig";

const {{pascalCase config.entity.name}}Inbox = () => {
  const { t } = useTranslation();
  const navigate = useNavigate();

  const configs = {{camelCase config.entity.name}}InboxConfig();

  return (
    <React.Fragment>
      <div className="digit-inbox-search-wrapper">
        <InboxSearchComposer configs={configs}></InboxSearchComposer>
      </div>
    </React.Fragment>
  );
};

export default {{pascalCase config.entity.name}}Inbox;